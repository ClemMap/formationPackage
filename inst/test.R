library(formationPackage)
?carre
carre(3)

library(formationPackage)
retard_sncf <- get_sncf()


library(formationPackage)
impute_na(c(2,1,1,2,NA), "moyenne")


library(covr)
package_coverage()
report()


# "happy git with R"
